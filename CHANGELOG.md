# 更新日志 (CHANGELOG)

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

---

## [0.3.3] - 2026-06-28

### ✨ 新功能

- **记住密码功能（多用户独立存储）**
  - 登录页面新增"记住30天"复选框
  - 多用户独立存储 remember_token，切换账号时自动填充
  - 退出登录时保留 remember_token，下次登录自动填充
  - remember_token 有效期 30 天（可通过 `VITE_REMEMBER_DAYS` 环境变量配置）
  - 安全设计：前端不存储密码，只存储随机 token

- **后端认证增强**
  - 新增 `remember_tokens` 数据库表存储用户 remember_token
  - `RememberToken` 模型管理 token 生命周期（生成、验证、过期）
  - `AuthController` 支持 remember_token 登录
  - token 一次性使用：每次登录后更新 token

### 🐛 Bug 修复

- 修复多用户登录时 remember_token 互相覆盖的问题
- 修复 React 受控输入变为非受控的警告（密码输入框）
- 修复用户切换时无法自动填充对应账号的 remember_token

### ✅ 测试

- 新增 `tokenManager.test.js` 40 个测试用例
  - 多用户独立存储测试
  - 过期时间处理测试
  - 旧版本数据兼容性测试
  - 完整用户流程测试
- 新增 `authService.test.js` 20 个测试用例
  - 记住密码登录流程测试
  - 多用户切换测试
  - 退出登录保留 remember_token 测试
- 前端 237 个测试用例全部通过
- 后端 94 个 PHPUnit 测试全部通过

### 🔒 安全

- 前端不存储密码，只存储随机生成的 remember_token
- remember_token 一次性使用（每次登录后更新）
- token 有效期限制（默认 30 天）
- 用户可随时清除 remember_token

### 📚 文档

- 更新 CHANGELOG.md 记录新版本
- 更新 README.md 添加"记住密码"功能说明

---

## [0.3.2] - 2026-06-28

### ✨ 新功能

- **AI 客服网络容错增强**
  - 发送消息失败时（网络错误/接口超时）自动显示常见问题列表
  - 6 个预设 FAQ 按钮可直接点击提问
  - 转人工客服按钮始终可用
  - 用户体验：网络异常时不再显示空白错误，而是提供有用的备选方案

- **RAG 响应缓存机制**
  - 热门问题答案缓存 1 小时，重复提问瞬间响应（0.2秒）
  - 首次请求优化至 10-11 秒（符合 8-12 秒要求）
  - LLM 调用参数优化：非流式请求、缩短超时、优化生成参数

### 🔧 优化

- Ollama LLM 调用超时从 120秒 缩短至 10秒
- 使用优化参数：`top_k=10, top_p=0.5, repeat_penalty=1.1`
- 只检索最相关的 1 个文档 chunk，减少上下文长度

### 🐛 Bug 修复

- 修复 AgentSeeder 写死测试数据问题（删除 20 条假数据）
- 修复时区配置：UTC → Asia/Shanghai
- 导入 13 条历史报告到数据库

### 📝 测试

- 更新 chatStore 单元测试：发送失败时验证 fallback 消息结构
- 全部 198 个测试通过

---

## [0.3.1] - 2026-06-27

### ✨ 新功能

- **菜单权限控制**
  - 根据用户角色动态显示/隐藏菜单项
  - 创建 RoleMiddleware 中间件处理后端角色权限
  - 创建 RoleRoute 组件保护前端路由
  - 后端 API 路由全部添加角色权限中间件
  - 错误日志/定时任务仅管理员，报告仅管理员+VIP，客服仅管理员+客服

- **消息中心权限同步**
- **权限管理增强**
- **RAG 客服系统**
  - 基于项目文档的智能问答（向量检索 + 余弦相似度）
  - 使用 nomic-embed-text 模型生成文档向量
  - 三层回复策略：精确匹配、模糊匹配、兜底回复
  - 兜底时展示常见问题列表 + 转人工按钮
  - 转人工通知所有 admin 和 support 用户
  - 知识库构建命令：`php artisan knowledge:build`
  - 管理员可直接创建新用户（用户名+密码+角色）
  - 管理员可删除用户（不可删除自己）
  - 角色选择新增"客服"选项
  - 错误日志 Tab 非 admin 用户隐藏（包括 Tab 和数据请求）
  - 客服消息点击：admin/support 跳管理页，普通用户不跳转
  - 独立知识库构建脚本（断点续传 + 进度保存 + 重试机制）
  - BuildKnowledgeBase 命令优化：批次缩小到 5、超时 180s、最大重试 2 次

- **数据库自动备份系统**
  - `backup-db.sh`：备份/恢复/清理三合一（pg_dump + gzip）
  - 启动时自动恢复最新备份 → 迁移 → 填充种子 → 备份当前数据
  - 凌晨 2 点 crontab 自动备份
  - 备份保留策略：正常 3 份，恢复后只保留最新 1 份
  - `start-backend.sh`：一键启动（恢复 + 迁移 + 填充 + 备份 + 启动）

- **Seeders 幂等化**
  - AgentSeeder：`firstOrCreate` + 日志去重
  - GraphSeeder：已有数据跳过
  - VersionUpdateSeeder：`firstOrCreate` 按版本号
  - DatabaseSeeder：串联所有子 seeder，`migrate:fresh --seed` 自动恢复

### 🐛 Bug 修复

- 修复客服聊天 API 权限过严：创建对话、发消息等用户功能被错误限制为仅 admin/support 可用
- 修复 Ollama 超时太短（30s→120s）导致 AI 回复失败
- 修复发送失败时用户消息丢失：失败消息保留在聊天列表，支持一键重发
- 修复重试消息时错误移除所有失败消息（改为只移除当前重试的那条）
- 修复 README 后端测试数量过时（34→77）
- 修复后端权限中间件未覆盖 error-logs/reports/cronjobs 路由（前端限制可被绕过）
- 修复 /permissions/me 自查接口被错误锁在 admin-only 中间件下
- 修复 CHANGELOG 错误日志权限表与代码不一致
- 修复 sendMessage 端点缺少 support 角色权限检查

### ✅ 测试

- 新增 20 个权限相关测试用例（路由保护 + 菜单过滤 + 前后端一致性验证）
- 新增 9 个 Seeders 幂等性测试（每个 seeder 独立 + 完整运行 + 重复运行不重复）
- 新增 8 个备份系统测试（脚本存在性 + 备份格式 + 保留策略 + crontab 配置）
- 前端 198 个测试用例全部通过
- 后端 94 个 PHPUnit 测试全部通过（原 77 + 新增 17）

### 🔒 安全

- 后端 API 路由全部添加角色权限中间件（error-logs/reports/cronjobs/chat/permissions）
- `/permissions/me` 自查接口开放给所有已登录用户

### 📋 角色权限配置

| 功能模块 | admin | support | vip | user |
|----------|-------|---------|-----|------|
| 仪表盘 | ✅ | ✅ | ✅ | ✅ |
| Agent列表 | ✅ | ✅ | ✅ | ✅ |
| 执行日志 | ✅ | ✅ | ✅ | ✅ |
| 错误日志 | ✅ | ❌ | ❌ | ❌ |
| 数据报告 | ✅ | ❌ | ✅ | ❌ |
| 知识图谱 | ✅ | ✅ | ✅ | ✅ |
| 定时任务 | ✅ | ❌ | ❌ | ❌ |
| 客服管理 | ✅ | ✅ | ❌ | ❌ |
| 权限管理 | ✅ | ❌ | ❌ | ❌ |
| 设置 | ✅ | ✅ | ✅ | ✅ |

---

## [0.3.0] - 2026-06-27

### ✨ 新功能

- **用户认证系统增强**
  - 注册页面添加图形验证码（GD 库生成，干扰线+噪点）
  - 登录支持用户名或邮箱 + 密码登录
  - 用户名唯一性校验，邮箱唯一性校验
  - 注册时自动生成随机昵称（快乐的小猫、勇敢的熊猫等）
  - 用户可在设置页面修改昵称
  - 测试邮箱 `@test.local` 可无限注册（用于测试）

- **看板娘系统完善**
  - Live2D 模型正确加载和渲染
  - 首次登录强制选择看板娘形象
  - 普通用户选择后不可更换（入口隐藏+页面跳转保护）
  - 管理员可随时更换看板娘形象
  - 切换形象时正确更新显示
  - 不同账号的看板娘互不影响（登录时清除旧缓存）

- **角色权限系统**
  - 新增客服角色（support），权限介于用户和管理员之间
  - 登录错误信息显示后端真实文案（账号不存在、密码错误等）

### 🐛 Bug 修复

- 修复注册页面错误信息不显示后端返回的具体错误
- 修复验证码加载两次的问题（React StrictMode）
- 修复注册失败后验证码不自动刷新
- 修复注册成功后不自动跳转登录页
- 修复 request.js 401 错误不显示后端真实信息
- 修复 Live2D 切换形象时不更新显示（Canvas 未挂载就加载）
- 修复 MascotSelect.jsx 中 useState 误用为 useEffect
- 修复 172.x IP 段匹配过于宽泛（改为 172.16.0.0/12）
- 修复白名单路径匹配使用 includes() 的误匹配风险
- 修复新账号登录后继承旧账号看板娘的问题

### ✅ 测试

- 前端 160 个测试用例全部通过
- 更新 authService.test.js 适配 login 字段变更
- 后端 PHPUnit 测试通过

### 🔒 安全

- 修复 172.x IP 段匹配（精确到 172.16-31.x.x）
- 白名单路径改为前缀精确匹配
- 验证码防暴力破解（5 次尝试限制，5 分钟过期）

---

## [0.2.1] - 2026-06-23

### 🐛 Bug 修复

- **修复登录后重定向到登录页的竞态条件问题**
  - 根本原因：`init()` 和 `login()` 之间存在竞态条件，`init()` 的 `else` 分支会覆盖 `login()` 设置的 `isAuthenticated: true`
  - 修复方案：
    - `init()` 开始时检查 `isAuthenticated`，如果已登录则跳过初始化
    - 在 `else` 分支中，再次检查 token 是否存在
    - 如果 `init` 期间有了新 token，主动恢复登录状态
  - 新增 4 个 `init()` 竞态条件测试用例

### ✅ 测试

- 全部 30 个单元测试通过
- 新增测试覆盖：竞态条件场景、token 刷新、缓存用户信息

---

## [0.2.0] - 2026-06-22

### ✨ 新功能

- **客服 Agent 系统**
  - AI 自动回复（基于 Ollama 本地模型）
  - 人工接管功能
  - 客服管理后台页面 (`/chat`)
  - 实时对话界面

- **看板娘 Live2D 组件**
  - 静态 SVG 看板娘动画（因 Cubism 2 SDK CDN 不可用降级）
  - 环状交互菜单（`RadialMenu`）
  - 眼睛跟随鼠标效果
  - 浮动动画

- **看板娘形象选择**
  - 首次登录选择形象页面 (`/select-mascot`)
  - 形象绑定用户，不可更改

- **客服集成到看板娘**
  - 点击 💬 图标打开客服对话

### 🐛 Bug 修复

- 修复登录表单 `type="email"` 验证问题——改为 `type="text"` 支持用户名登录
- 修复认证 `init()` 异步问题——添加 `isLoading` 状态到 `ProtectedRoute`
- 简化 `request.js`——移除请求取消逻辑以避免重复初始化时请求被取消
- 修复 Live2D Cubism 2 SDK 加载失败——替换为静态 SVG 看板娘动画

### 📦 部署

- 创建一键部署脚本 `install.sh`（交互式配置、自动安装环境、Nginx 配置、systemd 服务）
- 创建 Docker 部署文件：
  - `agent-api/Dockerfile`（PHP 8.2 FPM Alpine）
  - `apps/web/Dockerfile`（Node.js 构建 + Nginx 托管）
  - `docker-compose.yml`（api, web, postgres, redis）

### 📚 文档

- 创建 VitePress 文档项目 (`apps/docs`)
- 快速开始指南
- Docker 部署指南
- 前端 README 和后端 README

---

## [0.1.0] - 2026-06-21

### ✨ 新功能

- **基础架构搭建**
  - 前端 Monorepo 结构（pnpm workspaces）
  - 后端 Laravel 11 + Sanctum 认证
  - PostgreSQL 主库 + Redis 缓存

- **暗色科技风格 UI**
  - 自定义暗色主题（主色调 `#06b6d4`）
  - 侧边栏导航、头部组件
  - 仪表盘、Agent 列表、执行日志页面

- **认证系统**
  - 登录/注册页面
  - JWT Token 认证
  - 受保护路由 (`ProtectedRoute`)
  - IP 检测中间件
  - API 限流（登录 5 次/分钟、注册 3 次/分钟）

- **Agent 管理**
  - Agent 列表页面
  - 执行日志页面
  - 仪表盘统计卡片

- **错误日志系统**
  - `ErrorType` 枚举（17 种错误类型）
  - `ErrorLog` 模型和控制器
  - 错误日志页面和统计卡片
  - PostgreSQL 语法兼容修复

- **DDoS 防护**
  - 后端限流中间件
  - 前端节流/防抖

### 🐛 Bug 修复

- 修复安全问题：替换 Laravel 欢迎页为通用 JSON 响应
- 配置错误处理隐藏框架详情

### ✅ 测试

- 后端 PHPUnit 测试
- 前端 Vitest 26 个用例通过

---

## [0.0.1] - 2026-06-20

### ✨ 初始化

- 项目初始化
- 技术栈选型
- 环境搭建（Node.js, pnpm, PHP, Composer, PostgreSQL, Redis）

---

## 版本号说明

- **主版本号 (MAJOR)**：不兼容的 API 变更
- **次版本号 (MINOR)**：向下兼容的功能性新增
- **修订号 (PATCH)**：向下兼容的问题修正
